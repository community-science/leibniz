{
  "name": "@example-owner/leibniz-console",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "python -m leibniz.console.codegen",
    "prebuild": "npm run generate",
    "build": "tsc --noEmit && vite build",
    "precheck": "npm run generate",
    "check": "tsc --noEmit",
    "predev": "npm run generate",
    "dev": "vite",
    "pretest": "npm run generate",
    "test": "node ../../../../tests/run_console_web_tests.mjs && npm run browser-smoke",
    "browser-smoke": "node ../../../../tests/console_browser_smoke.mjs",
    "pretypecheck": "npm run generate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "lucide-react": "^1.8.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "playwright": "^1.60.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.9"
  }
}
